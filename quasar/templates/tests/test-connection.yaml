apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quasar.fullname" . }}-test-connection"
  labels:
    {{- include "quasar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quasar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
